spring:
  profiles:
    active:
      -dev
---
spring:
  profiles: dev
  application:
      name: microservice-config-dev-2
---
spring:
  profiles: pre
  application:
      name: microservice-config-pre